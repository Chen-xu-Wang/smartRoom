# 2026-08-27 工作记录

## 接入真实大模型（用户要求）
- 用户确认原 AI 是"假的人工对话"（关键词模拟），要求接入真实大模型，提供了 qnaigc 凭据：
  - base url: https://api.qnaigc.com/v1
  - apikey: sk-96c89e72...（已写入 backend/.env，已 gitignore，未进仓库）
- 新增 `backend/app/services/llm.py`：OpenAI 兼容 /chat/completions，标准库 urllib（无额外依赖），chat_json 提取 { } 容错，异常一律抛出由调用方 fallback。
- 改造 `backend/app/services/agent.py`：
  - extract_info（意图理解）与 _generate_work_order（工单分析）改为优先调 LLM，失败/超时/非JSON 自动兜底原关键词/规则逻辑。
  - 保留状态机、返回字段（agent_state/extracted_info/missing_info/tool_calls/work_order/rag_results/archive_data）与前端、A/B 落库契约（urgency中文、confidence数字、related_equipment设备id数组等）不变。
  - AI 回复标注"（大模型）"/"（规则）"，便于区分。
- backend/.env 启用 LLM 配置（默认 LLM_MODEL=deepseek-v3.1）。

## 验证与问题
- `is_llm_enabled=True` 配置生效；/models 端点正常（能列出 deepseek/qwen/kimi/glm 等模型）。
- **问题**：qnaigc 对该 key 的 `/chat/completions` 所有模型均返回 `502 service temporarily unavailable`（upstream_error），属于上游/chat 额度问题，非代码。
- fallback 生效：LLM 不可用时自动走规则，系统不崩、流程正常、A/B 契约不变（smoke 测试 by_llm=False，字段齐全）。
- 重启后端时旧 uvicorn(56292) 端口未释放导致新进程 WinError 10013；用 PowerShell Stop-Process 强杀后干净重启，nohup 跑 run.py，smoke 确认新代码生效。

## tokenhub 接入验证成功
- 用户提供 tokenhub.tencentmaas.com 端点 + sk-zeIQ5urum... key，hy3 模型可用（qnaigc 继续备用）。
- `.env` 切到 tokenhub，LLM_MODEL=hy3。
- 真实 payload（7设备+5知识条目）下 hy3 响应约 30-60s，超时从 30s 改为 90s。
- **最终验证结果**：
  - extract_info → 真实 LLM ✅（location/device/symptom/leak_after_close 全对）
  - _generate_work_order → 真实 LLM ✅（fault_type/ai_analysis/possible_causes/suggested_trade/urgency/confidence/related_equipment 全对）
  - `_by_llm: True` 确认 ✅
  - A→B 联调：32/32 项全部通过 ✅，契约完全对齐
- 前端 AI 回复标注（大模型）/（规则），用户可感知区别。
- debug 发现：Windows 无 `/tmp` 目录，用相对路径 `os.path.join(os.path.dirname(__file__), "..", "filename")`。

## 待办/提醒
- qnaigc key（sk-96c89e72...）备用，当前主用 tokenhub + hy3。
- 安全：API key 已出现在聊天记录中，如担心泄露建议轮换。
- README.md 仍是旧 SQLite 版，待更新（与队友同步）。
- 后端单进程启动（无 --reload）更稳定，避免 uvicorn 多进程树互相拉起。
